#include <iostream>

int main(int argc, **char argv) {
  for (char i = 0; (i) < (23); (i) += (1)) {
    if (((i) + (3)) == ((i) + (((i) + (2)) * (a)))) {
      (std::cout) << ((i) + ((a) * ((a) + (1))) + (b)) << (std::endl);
    }
  }
  for (auto &th : threads) {
    th.join();
  }
}
